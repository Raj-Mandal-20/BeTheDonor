
module.exports = {
  content: [
    "./src/**/*.{html,js,ejs}", // Include all HTML, JS, and EJS files in the `src` directory
    "./views/**/*.{html,js,ejs}", // Include all HTML, JS, and EJS files in the `views` directory
    "./public/**/*.html", // Include all HTML files in the `public` directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
